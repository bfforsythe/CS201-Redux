//Brandon Forsythe
//CS201-Redux HW04
//3/7/22




#include "std_lib_facilites.h"

bool ReadLine(string& str) {
	getline(cin, str);

	cout << str.empty();

	return(0);
}

unsigned StringtoTokenWS(const string& input, vector<string>& tokens) {
	
	string str;
	ReadLine(str);


	std::istringstream(fullString);

	return(0);
}

int main() {

	string str;
	vector<string> tokens;

	StringtoTokenWS(str, tokens);
}